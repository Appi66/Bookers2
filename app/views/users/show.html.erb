<h1>User詳細ページ　 Userの投稿を一覧表示</h1>
<%= render 'user', user: @user %>
<%= render 'books/book', book: @book %>


<h1>Books</h1>
 <table class="table table-striped">
 <thread>
  <tr>
   <th></th>
   <th>Title</th>
   <th>Opinion</th>
  </tr>
 </thread>
 <tbody>
  <% @books.each do |book| %>
  <tr>
   <td><%= image_tag book.user.get_profile_image("100x100") %></td>
   <td><%= book.title %></td>
   <td><%= book.body %></td>
  </tr>
  <% end %>
 </tbody>
</table>